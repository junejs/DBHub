# 15 — UI 页面清单与关键线框

> 前端开发依据。覆盖全部页面清单(按区域 + 角色 + 关键元素),以及核心页面的 ASCII 线框与交互状态约定。i18n 中英双语(D23);配色与组件遵循一套设计系统(本文给结构,视觉细节由设计稿定)。

---

## 1. 全局框架

```
┌─ DBHub ───────────────── [项目: orders ▾] [环境色标] [🔔 通知] [👤 alice ▾] ─┐
│ ┌─ 主导航(左) ─┐  ┌─ 内容区 ──────────────────────────────────────────────┐  │
│ │ 🏠 工作台     │  │                                                       │  │
│ │ 📦 资源       │  │                  (各页面)                            │  │
│ │ ⭐ 收藏       │  │                                                       │  │
│ │ 📤 导出中心   │  │                                                       │  │
│ │ 🛡 审计       │  │                                                       │  │
│ │ ⚙ 管理后台 ▾  │  │                                                       │  │
│ └───────────────┘  └───────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────────────┘
```
- **顶部栏**:项目切换器(仅本人所属项目)、当前环境色标、通知铃铛(未读数)、用户菜单(个人设置/切换语言/登出)。
- **主导航**按角色显隐:普通成员看前 4 项;`securityAdmin`/`workspaceAdmin` 多「审计」「管理后台」。
- **环境色标**:prod 🔴、stage 🟠、test 🔵、dev 🟢(取自 `environments.color`)。

---

## 2. 页面清单

| 区域 | 页面 | 主要角色 | 关键元素 |
|---|---|---|---|
| 认证 | 登录 | 所有人 | 账密 + SSO 按钮(IDP 列表)+ 忘记密码 |
| 认证 | SSO 回调 / MFA 二次验证 | 所有人 | OIDC 回调处理;MFA 输 OTP/恢复码 |
| 工作台 | **SQL 查询工作台** ⭐ | 查询者 | 对象树 + 编辑器 + 结果区(行数上限提示) |
| 工作台 | 查询历史 | 查询者 | 自身历史、搜索、重跑 |
| 工作台 | 保存的查询(Worksheet) | 查询者 | 项目/私有列表、可见性、编辑(etag) |
| 资源 | 项目选择 / 资源树 | 查询者 | 项目→实例→库→表/视图→列 |
| 资源 | 表详情 | 查询者 | 列定义、索引、外键、行数/大小、DDL、数据预览 |
| 收藏 | 我的收藏 | 查询者 | 搜索、一键打开 |
| 导出 | 导出弹窗 + 导出中心 | 查询者 | 选格式/密码;任务列表/状态/下载 |
| 权限 | 项目成员与角色(IAM) | projectOwner | 成员列表、加角色、结构化条件(环境/库/表) |
| 审计 | 审计查询 | securityAdmin/projectOwner | 结构化过滤、结果表、导出(本项目范围) |
| 通知 | 通知中心 | 所有人 | 未读/全部、跳转、标记已读 |
| 管理 | 实例管理(注册/测试连接) | projectDBA/projectOwner | 表单、ADMIN/RO 数据源、连通测试 |
| 管理 | 环境与策略 | securityAdmin/workspaceAdmin | 环境 CRUD、`environment_policies` 编辑 |
| 管理 | 身份提供商(IDP) | workspaceAdmin | OIDC/LDAP 配置、字段映射、测试 |
| 管理 | 用户 / 组 | workspaceAdmin | 用户、组、成员、状态 |
| 管理 | 平台设置 | workspaceAdmin | key/value、配额、限流参数 |
| 个人 | 个人设置 | 所有人 | 改密、MFA、语言、时区 |

---

## 3. 核心线框

### 3.1 SQL 查询工作台(最高频,核心页)

```
┌─ [proj: orders ▾]  pg-prod 🔴  orders_db ▾  schema: public ▾    [▶ 执行] [⇧ 执行选中] [⤓ 导出] ─┐
│ ┌─ 对象树 ──────┐ ┌─ 编辑器(Monaco + LSP) ─────────────────────┐ ┌─ 结果 ────────────────┐ │
│ │ 🔍 搜索表/列  │ │ 1  SELECT id, region                            │ │ Rows: 100           │ │
│ │ ▾ pg-prod 🔴  │ │ 2  FROM customers                              │ │ ┌───┬────────────────┐ │ │
│ │   ▾ orders_db │ │ 3  WHERE region = 'east'                 │ │ │id │ region         │ │ │
│ │     ▾ 表       │ │ 4  LIMIT 100;                                  │ │ ├───┼────────────────┤ │ │
│ │       customers│ │                                                │ │ │ 1 │ east           │ │ │
│ │       orders   │ │  (补全:输入 c.. → 提示 customers 列)          │ │ │ 2 │ west           │ │ │
│ │     ▾ 视图     │ │                                                │ │ │.. │ (更多...)      │ │ │
│ │ ▾ pg-test 🔵   │ └────────────────────────────────────────────────┘ │ 截断:行数达上限,导出 │ │
│ └────────────────┘                                                    └──────────────────────┘ │
│  [历史] [保存为Worksheet] [EXPLAIN]    状态: ✅ 100 行 · 0.42s · env prod            │
└────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- 对象树仅显示当前项目 + 本人可见库。
- 行数上限:结果达上限时截断并提示"改走导出"。
- 查询超时/只读违规 → 错误条明确指出(`QUERY_TIMEOUT` / `NON_READONLY_STATEMENT`)。

### 3.2 导出弹窗 + 导出中心

```
导出弹窗:                              导出中心:
┌─ 导出 orders_db ──────┐              ┌─ 导出中心 ───────────────────────────────────────┐
│ SQL:  [已有语句 ▾]    │              │ 状态: 全部 ▾   [+ 新建导出]                       │
│ 格式: CSV ▾           │              │ ──────────────────────────────────────────────── │
│ 行数上限: 100000      │   ──────▶    │ ✅ customers_export.csv  prod  10万行 2MB  ⬇下载  │
│ ZIP 密码: [••••••••]  │              │    ↳ 24h 后过期                                   │
│                      │              │ ⏳ orders_export.json    prod  运行中 40%         │
│ [取消] [开始导出]     │              │ ❌ big_table.csv         prod  失败(超时) [重试]  │
└───────────────────────┘              └──────────────────────────────────────────────────┘
```
- 同步导出预估 >1万行 → 弹窗提示"结果较大,已转为异步任务"(`EXPORT_TOO_LARGE_FOR_SYNC`)。
- 产物下载需输入 ZIP 密码;过期 → `EXPORT_ARCHIVE_EXPIRED`,下载按钮禁用。
- 完成推送站内通知(`export_done`)。

### 3.3 审计查询页

```
┌─ 审计 ─ [范围: 本项目 ▾ / 全局(securityAdmin)] ─ [导出 CSV/JSON/XLSX] ────────────────────────┐
│ 过滤: 操作=[SQL.Query ▾]  用户=[alice ▾]  时间=[上月 ▾]  ...                [应用]            │
│ ──────────────────────────────────────────────────────────────────────────────────────────── │
│ 时间           用户       操作              资源              状态  耗时                     │
│ 07-15 14:02   alice      SQL.Query        orders_db/customers 200  0.4s                     │
│ 07-15 13:55   bob        SQL.Export       orders_db/orders    200  12s                      │
│ 07-15 13:40   carol      SetIamPolicy     projects/orders     200  -     Δ+1 binding         │
│ ...                                                                                          │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```
- projectOwner 只能选"本项目";securityAdmin/workspaceAdmin 可选"全局"。
- 点击行展开 `request/response`(敏感字段已置空);权限变更行展示前后差异。
- 导出审计行为本身也被审计。

### 3.4 项目成员与角色(IAM)

```
┌─ 项目 orders · 成员与角色 ─ [编辑策略(etag)] ──────────────────────────────────────────────┐
│ 绑定列表                                                                                   │
│  角色                  成员                          条件(环境/库/表)          操作           │
│  sqlEditorReadUser     group:analysts@              env∈[dev,test,stage]      [编辑] [删]   │
│  projectDBA            user:dave@                   —                         [编辑] [删]   │
│  sqlEditorReadUser     user:alice@                  env=prod,db=orders_db     [编辑] [删]   │
│ [+ 添加绑定]  角色 ▾  成员 ▾  条件 ▾(结构化: 环境/库/表 多选)                              │
└────────────────────────────────────────────────────────────────────────────────────────────┘
```
- 结构化条件编辑器提供环境/库/表多选与校验。
- 保存走 `SetIamPolicy` + etag;并发冲突 → `CONCURRENT_MODIFICATION` 提示刷新重试。

### 3.5 JIT 审批 — 不在 v1 范围

> v1 不做平台侧 JIT，无 JIT 审批页（路线图见 [18 §1.2](./18-roadmap.md)）。

### 3.6 实例注册 / 测试连接

```
┌─ 注册实例(项目 orders) ────────────────────────────────────────────────┐
│ 项目: orders(自动)    环境: prod 🔴 ▾(必选)    引擎: PostgreSQL ▾       │
│ 显示名: pg-prod                                                         │
│ ── ADMIN 数据源(恰好 1)──                                              │
│   host: 10.0.0.5  port: 5432  database: postgres  username: bb_sync     │
│   密码: [••••••]  或 [外部 Secret Manager 引用]                          │
│   ☐ SSL   ☐ SSH 隧道                                                    │
│ ── READ_ONLY 数据源(可选 1)──  [折叠]                                  │
│ 同步周期: 15min                                                          │
│ [测试连接(validate_only)]   [取消]   [创建]                              │
└─────────────────────────────────────────────────────────────────────────┘
```
- 测试连接不落库;失败显示引擎错误(`VALIDATE_ONLY_FAILED`)。
- 密码不入明文(AES-256-GCM,D25)。

---

## 4. 交互状态约定(全站通用)

| 状态 | 表现 |
|---|---|
| 加载 | 骨架屏 / spinner;长操作(导出/同步)显示进度 |
| 空 | 空状态插画 + 引导(如"无可见库,联系 projectOwner") |
| 错误 | 顶部错误条 + 结构化信息(错误码 + 建议 action) |
| 结果过大 | 截断提示 + "改走导出"按钮 |
| 环境色标 | 全站统一(prod 红...) |
| 乐观锁冲突 | 提示"内容已被他人修改,刷新后重试"(`CONCURRENT_MODIFICATION`) |
| 限流 | 提示"操作过于频繁,稍后重试"(`RATE_LIMITED`) |
| 通知 | 右上铃铛红点 + 下拉列表 + 站内通知中心页 |

---

## 5. 线框交付说明

- 本文为**结构线框**(信息架构 + 关键交互),非视觉设计稿。视觉(配色细节、间距、组件库)由设计稿定,建议基于一套组件库(如 Ant Design / shadcn)。
- 所有文案走 i18n key(中英,D23)。
- 响应式:工作台为主桌面端;审计/导出中心/管理后台桌面优先,移动端不强制。
