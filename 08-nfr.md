# 08 — 非功能性需求、风险与验收

> 本文档定义系统的非功能性需求（NFR）、主要风险与对策，以及整体验收标准。产品功能范围见各专题文档与 [00-overview.md](./00-overview.md) §5。

---

## 1. 非功能性需求（NFR）

### 1.1 性能

| 指标 | 目标 |
|---|---|
| 自动补全首字符 P95 | ≤ 200ms |
| 常见查询首屏 P95 | ≤ 2s（不含到业务库的固有网络延迟） |
| 元数据同步单库 | ≤ 30s（中等规模库） |
| 异步导出 | 支持百万级行，流式不 OOM |
| 审计查询（千万级） | 秒级（命中索引） |

### 1.2 可用性
- 单节点部署即可满足（单组织自部署、规模可控）。
- 单实例/库故障不影响其他库访问（连接隔离、错误隔离）。
- 元数据同步失败不阻塞查询（用缓存 + 标记 sync_status）。
- **可选增强（不在本期必须范围）：** 服务无状态多副本水平扩展、平台元数据库主从 + 定期备份、平台自身高可用。

### 1.3 安全
- 默认拒绝（Default Deny）。
- 职责分离（DBA / SecurityAdmin）。
- 连接凭据加密存储 / 对接 Secret Manager。
- 全链路 TLS；SSH 隧道接入。
- 脱敏服务端强制；谓词列保护生效。
- 审计不可篡改；密码/token/凭据等敏感字段自动脱敏（SQL 字面量按决策原样留存，见 [06](./06-audit-log.md)）。
- 防爆破限频；MFA；密码 bcrypt。

### 1.4 合规
- 审计满足「谁、何时、对什么、做了什么、结果」可回溯。
- 数据分类与脱敏支持敏感数据合规（个保法 / PII）。
- 导出留痕、产物密码保护、保留期可控。

### 1.5 可观测性
- 结构化日志（slog）；审计可镜像到 stdout 供 SIEM。
- 指标埋点：查询耗时/行数/错误率、同步状态、任务队列、连接池。
- 链路追踪（可选，Trace ID 贯穿 RPC→Driver）。

### 1.6 可维护性 / 可扩展性
- **引擎插件化**：新增引擎 = 新增 driver + parser 实现，不改核心。
- IdP 插件化：新增协议 = 新增 idp 插件。
- 权限/脱敏策略均为数据驱动配置，无需改代码即可调整。

### 1.7 国际化与本地化
- UI 中/英双语；时间/时区正确处理。

### 1.8 部署
- 单机 Docker / docker-compose / 二进制，单节点 + 平台 PG 即可运行。
- 提供 Helm Chart 形态以便未来集群化。

---

## 2. 风险与对策

| 风险 | 对策 |
|---|---|
| 多引擎解析器维护成本 | 当前版本聚焦 PostgreSQL；通过能力矩阵声明，解析失败时关闭细粒度能力并告警 |
| 审计量爆炸 | 冷热分层、归档、保留期策略、外部不可变存储（可选增强） |
| 大查询/导出拖垮业务库 | 查询成本护栏（EXPLAIN 阈值拦截）、语句超时、并发限制、流式 |
| 凭据泄露 | Secret Manager、内存用后即清、最小权限 DB 账号 |
| 脱敏被绕过（谓词列/导出/错误信息） | 谓词列检查、导出去脱敏需审批/JIT、错误信息脱敏 |
| 审计字面量含 PII | 审计读权限严格收紧（仅安全管理员/合规）、审计访问本身被审计、按保留期清理 |

---

## 3. 验收标准

1. 可注册 **PostgreSQL** 实例并完成元数据同步。
2. 用户经 OIDC/LDAP SSO 登录，组映射生效。
3. 工作台可对授权库执行查询，自动补全可用，结果正确分页。
4. **查询成本护栏生效**：高开销查询被拦截/告警，原因可追溯。
5. 敏感列在查询/导出结果中按策略脱敏；谓词列查询被拒。
6. JIT 临时访问可申请、审批、生效、过期。
7. SQL 收藏与分享可用：分享的 SQL 执行时按接收者本人权限/脱敏校验。
8. 同步/异步导出可用，产物密码保护，24h 过期清理。
9. 所有关键操作可在审计中心按 CEL 查询与导出（SQL 按字面量留存）。
10. 权限默认拒绝；职责分离（DBA 不可见脱明明文）成立。
11. **扩展性验证**：驱动/解析器/补全为插件化接口，新增引擎无需改动核心（以接口契约 + PostgreSQL 实现为证）。
