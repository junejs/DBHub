# 08 — 非功能性需求、风险与验收

> 本文档定义系统的非功能性需求（NFR）、主要风险与对策，以及整体验收标准。产品功能范围见各专题文档与 [00-overview.md](./00-overview.md) §5。

---

## 1. 非功能性需求（NFR）

### 1.1 性能

| 指标 | 目标 |
|---|---|
| 自动补全首字符 P95 | ≤ 200ms |
| 常见查询首屏 P95 | ≤ 2s（不含到业务库的固有网络延迟） |
| 元数据同步单库 | ≤ 30s（中等规模库） |
| 异步导出 | 支持百万级行，流式不 OOM |
| 审计查询（千万级） | 秒级（命中索引） |

### 1.2 可用性
- **MVP：单节点部署，不做备份**（D-risk-1，验证期可接受）。
- 单实例/库故障不影响其他库访问（连接隔离、错误隔离）。
- 元数据同步失败不阻塞查询（懒刷新，用缓存 + 标记 sync_status）。
- **可选增强 / 上线前建议：** 至少加平台 PG 的 pg_dump 定时备份；后续演进到多副本 + 主从。
- ⚠️ **已知风险**：平台 PG 含用户/权限/审计，单点丢失=全丢且审计不可恢复。上线前务必补备份。

### 1.3 安全
- 默认拒绝（Default Deny）。
- 职责分离（DBA / SecurityAdmin）。
- 连接凭据**应用层 AES-256-GCM 加密**，主密钥从环境变量/KMS 注入；支持 `secret_ref` 对接外部 Secret Manager（D25）。
- 全链路 TLS；SSH 隧道接入。
- 查询行数/超时/字节上限生效，保护业务库。
- 审计不可篡改；密码/token/凭据等敏感字段自动脱敏（SQL 字面量按决策原样留存，见 [06](./06-audit-log.md)）。
- 防爆破限频；MFA；密码 bcrypt。

### 1.4 合规
- 审计满足「谁、何时、对什么、做了什么、结果」可回溯。
- 敏感列可见性由数据库授权控制；全量审计支持合规回溯（个保法 / PII）。应用层脱敏延后 v2。
- 导出留痕、产物密码保护、保留期可控。

### 1.5 可观测性
- 结构化日志（slog）；审计可镜像到 stdout 供 SIEM。
- 指标埋点：查询耗时/行数/错误率、同步状态、任务队列、连接池。
- 链路追踪（可选，Trace ID 贯穿 RPC→Driver）。

### 1.6 可维护性 / 可扩展性
- **聚焦 PostgreSQL**：v1 仅 PG；未来新增引擎时抽取 driver/parser 接口，不改核心。
- IdP 可扩展：新增协议 = 新增 idp 实现。
- 权限策略为数据驱动配置，无需改代码即可调整。

### 1.7 国际化与本地化
- **中英双语 i18n**（D23，MVP 即出两套文案）；时间/时区正确处理。

### 1.8 部署
- 单机 Docker / docker-compose / 二进制，单节点 + 平台 PG 即可运行。
- 提供 Helm Chart 形态以便未来集群化。

---

## 2. 风险与对策

| 风险 | 对策 |
|---|---|
| 多引擎扩展（未来） | v1 仅 PostgreSQL；接入新引擎时再抽取解析器抽象，按需实现 |
| 审计量爆炸 | 冷热分层、归档、保留期策略、外部不可变存储（可选增强） |
| 大查询/导出拖垮业务库 | 语句超时、行数/字节上限、并发限制、流式 |
| 凭据泄露 | Secret Manager、内存用后即清、最小权限 DB 账号 |
| 审计字面量含 PII | 审计读权限按 D26 收紧（securityAdmin/workspaceAdmin 全量、projectOwner 本项目）、审计访问本身被审计、按保留期清理 |

---

## 3. 验收标准

1. 可注册 **PostgreSQL** 实例并完成元数据同步。
2. 用户经 OIDC/LDAP SSO 登录，组映射生效。
3. 工作台可对授权库执行查询，自动补全可用，结果正确分页。
4. 查询受语句超时 + 行数/字节上限保护；越权/跨项目访问被拒。
5. SQL 收藏与分享可用：分享的 SQL 执行时按接收者本人权限校验。
6. 同步/异步导出可用，产物密码保护，24h 过期清理。
7. 所有关键操作可在审计中心按结构化条件查询与导出（SQL 按字面量留存）。
8. 权限默认拒绝；职责分离（DBA 管连接、安全管理员管审计/策略）成立。
9. **扩展性验证**：PostgreSQL 驱动/解析器/补全为独立模块，接入新引擎时可抽取接口、不改核心（以模块边界 + PG 实现为证）。
