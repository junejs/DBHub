# 00 — 产品总览

## 1. 背景

组织内部的数据分散在多种数据库引擎中（PostgreSQL、MySQL、Oracle、Redshift、ClickHouse、MongoDB 等）。当前数据访问面临以下痛点：

1. **访问入口割裂**：每个人使用各自的数据库客户端（DBeaver、DataGrip、命令行等），连接凭据四处散落，难以集中管理。
2. **权限粒度粗**：通常只能到「数据库账号」级别，无法做到表级、列级的细粒度授权；DBA 通过手工 grant/revert 管理，易遗漏、易失控。
3. **缺乏审计**：谁在什么时间查询/导出了什么数据、尤其是敏感数据，缺乏完整、不可篡改的记录，无法满足合规要求。
4. **敏感数据暴露风险**：身份证号、手机号、银行卡号等字段在查询结果与导出文件中明文暴露。
5. **身份不统一**：组织已有企业身份系统（LDAP/AD、OIDC IdP），但数据库账号与人员身份脱节，离职/调岗后账号回收滞后。

## 2. 愿景

构建一个 **集中式、受控、可审计** 的数据库访问工作台。用户登录后，在统一界面中选择被授权的数据库，编写 SQL（支持自动补全）进行查询，或发起数据导出任务；每一次访问都经过精确的权限校验、敏感数据动态脱敏，并写入不可篡改的审计日志。

## 3. 目标用户

| 角色 | 典型身份 | 核心诉求 |
|---|---|---|
| **数据分析师/业务方** | Analyst、运营、产品 | 自助查询数据、导出报表，不需要直接接触数据库 |
| **开发工程师** | 后端/前端开发 | 联调排查时查询线/测试库数据，查看 schema |
| **DBA / 数据平台工程师** | DBA | 管理数据库实例与连接、配置权限策略、脱敏规则、排查问题 |
| **安全管理员** | Security、合规 | 审查审计日志、配置敏感数据分类与脱敏、审批临时访问 |
| **系统管理员** | 平台负责人 | 配置 IdP 集成、用户/组管理、环境与项目管理 |

> 关键设计原则：**职责分离（SoD）**。安全管理员与 DBA 角色分离，DBA 不能既配置脱敏规则又查看明文数据。

## 4. 核心使用场景

### 场景 A：自助 SQL 查询（最高频）
1. 用户通过 SSO 登录系统。
2. 在工作台选择一个数据库（系统仅展示其有访问权的数据库）。
3. 在 SQL 编辑器中编写查询，编辑器基于该库的 schema 提供 **表/列/函数的自动补全**。
4. 执行查询，结果分页展示；若结果包含敏感列，按脱敏策略自动掩码（如手机号显示为 `138****1234`）。
5. 查询语句被记录到该用户的查询历史，并写入审计日志。

### 场景 B：数据导出
1. 用户选择数据库，填写导出 SQL 与格式（CSV / JSON / SQL / XLSX）。
2. 若数据量小：同步导出，立即下载带密码的压缩包。
3. 若数据量大：提交异步导出任务，任务完成后在通知中心下载；任务产物有保留期（默认 24h）。
4. 导出结果同样遵循脱敏策略；导出行为完整记入审计。

### 场景 C：临时访问敏感数据（JIT）
1. 分析师需查看某用户的明文手机号用于客诉处理，常规权限被脱敏。
2. 发起「临时访问申请」，说明事由、目标列、有效期（如 30 分钟）、是否允许导出。
3. 审批人（安全管理员/DBA）批准，临时权限激活；过期自动失效。
4. 该期间的查询/导出去脱敏，且每一次都强审计。

### 场景 D：合规审计回溯
1. 安全管理员在审计中心按「操作类型=导出、时间=上月、用户=某分析师」筛选。
2. 查看每一条审计记录的：操作人、IP、UA、时间、目标库表、SQL 摘要、脱敏状态、是否经 JIT 授权。
3. 一键导出审计结果用于合规报告。

## 5. 范围与边界

### 5.1 产品范围（完整交付）

| 能力域 | 说明 |
|---|---|
| 资源管理 | Instance / Database / Schema / Table / View / Column 的发现、同步与展示 |
| SQL 查询工作台 | 多语句执行、自动补全（LSP）、结果分页、查询历史、EXPLAIN、查询成本护栏 |
| 数据导出 | 同步 + 异步导出，多格式，密码压缩包 |
| 权限体系 | RBAC + IAM 绑定 + CEL 条件；库 / 表 / 列（脱敏）权限 + JIT 临时访问 |
| 认证集成 | 本地账号/密码、LDAP、OIDC、SSO、JIT provisioning、组映射、MFA |
| 审计日志 | 全量关键操作审计、不可篡改、可查询导出 |
| SQL 收藏与分享 | 个人收藏夹、基于 Worksheet 可见性的安全分享 |
| 数据库引擎支持 | **架构支持多种数据库；当前版本仅实现 PostgreSQL**（见 §5.2） |

### 5.2 数据库引擎支持策略（重要）

> **本系统设计上支持多种数据库引擎，当前版本仅实现 PostgreSQL。**

- 核心逻辑（连接、查询、导出、权限、脱敏、审计、自动补全）均通过**插件化抽象**实现引擎无关：数据库驱动（`Driver`）、SQL 解析器（`Parser`/`Completion`）、脱敏实现各自按引擎注册。
- **当前版本仅交付 PostgreSQL 引擎**的驱动、解析器与自动补全实现。
- 新增引擎（MySQL、Oracle、Redshift、ClickHouse、Snowflake、MSSQL、MongoDB 等）只需实现对应的驱动/解析器插件并注册，**无需改动核心代码**——扩展性是架构的一等目标。
- 每个引擎通过**能力矩阵**声明其支持的功能（查询新 ACL / 脱敏 / 自动补全 / 成本护栏），能力不足时优雅降级。

### 5.3 明确不做（产品边界）
- 不直接作为数据库的高可用/备份/迁移工具。
- 不替代业务系统的 OLTP 入口（仅面向人/BI 的查询与导出）。
- 不实现数据库账号的自主开通与自助密码修改（由 DBA 管理）。
- **不做数据库变更管理**（schema 变更审批、迁移、SQL Review 等）——属未来延伸方向，架构仅预留抽象，不在本 PRD 交付范围。
- **不做行级权限（Row-Level Security）**——其 SQL 改写注入正确性风险高、安全代价大，未来若确有需求优先走数据库原生 RLS。

## 6. 核心设计原则

1. **默认最小权限（Default Deny）**：任何未被显式授权的资源，默认不可访问。
2. **职责分离（SoD）**：DBA 不掌握脱敏规则的查看明文能力；安全管理员不掌握连接凭据。
3. **全程可审计（Audit by Default）**：关键操作通过注解声明，由拦截器统一记录，业务代码无法绕过。
4. **敏感数据不出明文（Dynamic Masking）**：脱敏在服务端结果层强制执行，客户端无法绕过；谓词列（WHERE 中的敏感列）默认禁止以防止通过过滤推断明文。
5. **引擎无关的抽象（Plugin Architecture）**：数据库连接、SQL 解析、自动补全、脱敏均通过插件接口按引擎实现，新增引擎不污染核心逻辑。

## 7. 成功指标（参考）

| 指标 | 目标 |
|---|---|
| 数据库引擎 | 架构支持多种；**当前版本实现 PostgreSQL** |
| 权限最细粒度 | 列级（动态脱敏）+ 表/库级（CEL 授权） |
| 审计覆盖率 | 所有关键操作 100% 记录，无绕过路径 |
| 单库 schema 同步延迟 | 自动同步周期内（≤ 15 min），手动触发可即时刷新 |
| 查询自动补全首字符响应 | P95 ≤ 200ms |
| 导出能力 | 单任务支持百万级行导出（异步） |

## 8. 术语表

| 术语 | 定义 |
|---|---|
| **Instance（实例）** | 一个物理数据库连接（host:port + 引擎类型 + 连接凭据），是系统注册的资源 |
| **DataSource（数据源）** | Instance 下的连接配置，分 ADMIN（管理连接，用于同步/变更）与 READ_ONLY（只读连接，用于查询） |
| **Database（数据库）** | Instance 内的逻辑库 |
| **Catalog（目录）** | 数据库的 schema 元数据（表/列/视图等）+ 平台侧标注（分类、脱敏语义类型） |
| **Project（项目）** | 数据库的归属容器，是权限授予（IAM）的基本单元 |
| **Environment（环境）** | 软标签（prod/test/dev），驱动策略选择，支持继承 |
| **IAM Policy** | 类 GCP IAM 的权限绑定模型：`角色 × 成员 × (可选)CEL 条件` |
| **CEL 条件** | 用 CEL 表达式将权限精确限定到具体库/表/列/环境 |
| **Query Span** | 对 SQL 语句做静态分析得到的「读/写了哪些列、谓词列是哪些」的语义结构，用于细粒度授权 |
| **Data Masking（动态脱敏）** | 在查询结果返回前，依据脱敏策略对敏感列做掩码改写 |
| **JIT Access（临时访问）** | Just-In-Time，按需申请、限期失效的临时授权（可含去脱敏） |
| **Audit Log（审计日志）** | 不可篡改的操作流水 |
| **LSP** | Language Server Protocol，SQL 自动补全的协议基础 |
